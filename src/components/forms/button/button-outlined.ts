import { ColorScheme } from './types';

export const buttonOutlined: Record<ColorScheme, string> = {
  blue: 'border border-blue-500 text-blue-500 hover:opacity-75 focus:ring focus:ring-blue-200 active:opacity-[0.85]',
  red: 'border border-red-500 text-red-500 hover:opacity-75 focus:ring focus:ring-red-200 active:opacity-[0.85]',
  green:
    'border border-green-500 text-green-500 hover:opacity-75 focus:ring focus:ring-green-200 active:opacity-[0.85]',
};
